using System;
using System.Reactive.Disposables;
using Unity;
using Unity.Lifetime;

namespace Comms
{
    // public static class DisposableListExt
    // {
    //     public static void AddToDisposableList(this IUnityContainer container, IDisposable disposable)
    //     {
    //         container.Resolve<IDisposableList>().Add(disposable);
    //     }
    //     public static void AddToDisposableList(this IUnityContainer container, Action act)
    //     {
    //         container.Resolve<IDisposableList>().Add(Disposable.Create(act));
    //     }
    // }
}